#ifndef WORK_H
#define WORK_H

#include <stdlib.h>
#include <time.h>

void    fill_array(int* array, size_t size, size_t k);
void    swap_row_column(int* array, size_t size, size_t k);

#endif // WORK_H
